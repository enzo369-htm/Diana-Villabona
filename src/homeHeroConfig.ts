/** Modo del hero en home: cambiá acá para probar cada versión. */
export type HomeHeroMode = "static" | "split" | "horizontal-carousel";

export const HOME_HERO_MODE: HomeHeroMode = "horizontal-carousel";
